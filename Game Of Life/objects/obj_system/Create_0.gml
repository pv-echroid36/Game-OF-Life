
			Grid = ds_grid_create(GRID_SIZE , GRID_SIZE);
            Grid_ = ds_grid_create(GRID_SIZE , GRID_SIZE);
            
            neighbors = 0;

            cycle = 2;
            alarm[0] = cycle;

            pause = false;





